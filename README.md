# dotfiles

### Installation
- Install necessary softwares before setup dotfiles. <TODO: Add guide>
- Run `source install.sh` to configure git, zsh, tmux and neovim
- Open neovim. Plugins will be automatically installed by lazy.nvim on first launch
